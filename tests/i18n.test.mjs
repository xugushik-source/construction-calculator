import test from "node:test";import assert from "node:assert/strict";import {tr} from "../lib/i18n.ts";
test("uses natural contractor terminology",()=>{assert.equal(tr("Estimate","ru"),"Смета");assert.equal(tr("Markup","es"),"Recargo");assert.equal(tr("Profit","ru"),"Прибыль");assert.equal(tr("Client total","es"),"Total cliente")});
test("switches translated labels back to English",()=>{assert.equal(tr("Прибыль","en"),"Profit");assert.equal(tr("Margen objetivo, %","en"),"Target margin %")});
test("uses natural estimate-item terminology",()=>{assert.equal(tr("New estimate item","ru"),"Новая позиция сметы");assert.equal(tr("Labor cost per unit","es"),"Mano de obra por unidad");assert.equal(tr("Masonry","ru"),"Кладочные работы");assert.equal(tr("Tiling","es"),"Alicatado")});
test("translates the inline client flow",()=>{assert.equal(tr("Add new client","ru"),"Добавить нового клиента");assert.equal(tr("Project address","ru"),"Адрес объекта");assert.equal(tr("Client name","es"),"Nombre del cliente")});
test("translates catalog hierarchy and flooring work",()=>{assert.equal(tr("Interior works","ru"),"Внутренние работы");assert.equal(tr("Flooring","es"),"Pavimentos");assert.equal(tr("Install parquet board","ru"),"Монтаж паркетной доски");assert.equal(tr("Install linoleum","es"),"Colocación de linóleo")});

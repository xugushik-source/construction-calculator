export type EstimateItemInput={quantity:number;laborRate:number;materialRate:number;markup?:number;targetMargin?:number};
export type EstimateInput={items:EstimateItemInput[];additionalExpenses?:number;discount?:number;tax?:number};
export function calculateEstimate(input:EstimateInput){const laborCost=input.items.reduce((s,i)=>s+Math.max(0,i.quantity)*Math.max(0,i.laborRate),0);const materialCost=input.items.reduce((s,i)=>s+Math.max(0,i.quantity)*Math.max(0,i.materialRate),0);const itemSales=input.items.reduce((s,i)=>{const c=Math.max(0,i.quantity)*(Math.max(0,i.laborRate)+Math.max(0,i.materialRate));if(i.targetMargin!==undefined){const m=Math.min(.99,Math.max(0,i.targetMargin));return s+c/(1-m)}return s+c*(1+Math.max(0,i.markup??0))},0);const otherCost=Math.max(0,input.additionalExpenses??0),totalCost=laborCost+materialCost+otherCost,subtotal=itemSales+otherCost,discount=Math.min(subtotal,Math.max(0,input.discount??0)),beforeTax=subtotal-discount,tax=beforeTax*Math.max(0,input.tax??0),sellingPrice=beforeTax+tax,profit=sellingPrice-tax-totalCost,margin=sellingPrice-tax>0?profit/(sellingPrice-tax):0;return{laborCost,materialCost,otherCost,totalCost,subtotal,discount,tax,sellingPrice,profit,margin}}
